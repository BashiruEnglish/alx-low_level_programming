#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: first number to multiply
 * @y: second number to multiply
 * Return: the result of multiplication between parameter x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
